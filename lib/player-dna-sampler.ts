import type {AnteFormat,GameMode,PlayerAction,PlayerDnaSpot} from "@/data/player-dna-spots";
import runtimeBankJson from "@/data/texassolver-runtime-bank.json";
import {buildExactComboSession,exactComboBankCapacity,type ExactComboBank} from "@/lib/player-dna-exact-combo-session";
import {sampleSolverAction,solverActionLabel,solverNodePolicy} from "@/lib/gto-range-policy";
import {comboKey,conditionRangeOnNodeAction,expandWeightedRange,sampleWeightedCombo} from "@/lib/poker-combo-range";

type Street=PlayerDnaSpot["street"];
type PriorAnswer={action:PlayerAction};
type StackBand="SHORT"|"MEDIUM"|"DEEP";
type Heads="HEADS-UP"|"MULTIWAY";
type PositionState="IP"|"OOP";
type PotType="LIMPED"|"SRP"|"3BET"|"4BET"|"OTHER";
type Theme="VALUE"|"BLUFF"|"BLUFF-CATCH"|"BLIND-WAR"|"SQUEEZE"|"ALL-IN"|"DRAW"|"PRESSURE"|"STANDARD";
type Texture="PREFLOP"|"DRY"|"WET"|"PAIRED"|"MONOTONE"|"CONNECTED";
type Sizing="SMALL"|"MEDIUM"|"LARGE"|"OVERBET"|"NONE";
export type SpotDimensions={street:Street;heroPosition:string;stackBand:StackBand;heads:Heads;positionState:PositionState;potType:PotType;theme:Theme;texture:Texture;sizing:Sizing;tournamentPhase:"EARLY"|"MID"|"BUBBLE"|"ITM"|"FT"|"NA";icm:boolean;ante:boolean;gameProfile:string;anteMode:AnteFormat};
const SUITS=["♠","♥","♦","♣"];
const MULTIWAY_INTERVAL=4;
const runtimeExactBank=runtimeBankJson as unknown as ExactComboBank;
function runtimeBankReady(){return runtimeExactBank.nodeCount===32&&runtimeExactBank.exactComboDecisionCount>=6456&&runtimeExactBank.rejectedBoardCollisionCombos===0}
export function runtimeSolverCapacity(mode?:GameMode){return runtimeBankReady()?exactComboBankCapacity(runtimeExactBank,mode):0}
function hashText(value:string){let hash=2166136261;for(let i=0;i<value.length;i++){hash^=value.charCodeAt(i);hash=Math.imul(hash,16777619)}return hash>>>0}
function randomFrom(seed:number){let state=(seed||1)>>>0;return()=>{state=(Math.imul(state,1664525)+1013904223)>>>0;return state/4294967296}}
function has(spot:PlayerDnaSpot,text:string){return spot.scenario.some(item=>item.toUpperCase().includes(text))}
function hero(spot:PlayerDnaSpot){return spot.players.find(player=>player.hero)??spot.players[0]}
function round(value:number,mode:GameMode){if(value<=0)return 0;return mode==="TORNEIO"?Math.max(.1,Math.round(value*10)/10):Math.max(1,Math.round(value))}
function capCommitment(value:number,stack:number,mode:GameMode){return round(Math.min(Math.max(0,value),Math.max(0,stack)),mode)}
function stableShuffle<T>(items:T[],seed:number){const out=[...items];const random=randomFrom(seed);for(let i=out.length-1;i>0;i--){const j=Math.floor(random()*(i+1));[out[i],out[j]]=[out[j],out[i]]}return out}
function splitCards(text?:string){return(text??"").split(" ").filter(Boolean)}
function normalizedAction(action:string):PlayerAction{const a=action.toUpperCase();if(a.includes("ALL-IN"))return"ALL-IN";if(a.includes("RAISE")||a.includes("3-BET")||a.includes("4-BET")||a.includes("SQUEEZE"))return"RAISE";if(a.includes("BET")||a.includes("OVERBET"))return"BET";if(a.includes("CALL"))return"CALL";if(a.includes("FOLD"))return"FOLD";return"CHECK"}
function suitPermutation(random:()=>number){const suits=[...SUITS];for(let i=suits.length-1;i>0;i--){const j=Math.floor(random()*(i+1));[suits[i],suits[j]]=[suits[j],suits[i]]}return new Map(SUITS.map((s,i)=>[s,suits[i]]))}
function remapCard(card:string,map:Map<string,string>){if(!card)return card;const rank=card.slice(0,-1),suit=card.slice(-1);return `${rank}${map.get(suit)??suit}`}
function remapCards(text:string|undefined,map:Map<string,string>){if(!text)return undefined;return text.split(" ").filter(Boolean).map(card=>remapCard(card,map)).join(" ")}
export function describeSpot(spot:PlayerDnaSpot):SpotDimensions{const h=hero(spot);const board=(spot.board??"").split(" ").filter(Boolean);const boardRanks=board.map(card=>card[0]);const boardSuits=board.map(card=>card.slice(1));const maxAction=Math.max(0,...spot.players.filter(player=>!player.hero).map(player=>player.value));const ratio=maxAction/(spot.pot.main||1);const paired=new Set(boardRanks).size<boardRanks.length;const monotone=boardSuits.length>=3&&new Set(boardSuits).size===1;return{street:spot.street,heroPosition:h.position,stackBand:h.stack<=30?"SHORT":h.stack<=80?"MEDIUM":"DEEP",heads:spot.players.filter(player=>player.hero||player.action!=="FOLD").length>2?"MULTIWAY":"HEADS-UP",positionState:has(spot,"OOP")?"OOP":"IP",potType:has(spot,"4-BET")?"4BET":has(spot,"3-BET")?"3BET":has(spot,"LIMP")?"LIMPED":has(spot,"SRP")?"SRP":"OTHER",theme:has(spot,"BLUFF CATCH")?"BLUFF-CATCH":has(spot,"VALUE")?"VALUE":has(spot,"BLIND WAR")?"BLIND-WAR":has(spot,"SQUEEZE")?"SQUEEZE":spot.players.some(player=>player.action==="ALL-IN")?"ALL-IN":has(spot,"DRAW")||has(spot,"COMBO")?"DRAW":has(spot,"ICM")||has(spot,"BOLHA")?"PRESSURE":"STANDARD",texture:spot.street==="PREFLOP"?"PREFLOP":paired?"PAIRED":monotone?"MONOTONE":has(spot,"DRAW")||has(spot,"COMBO")?"WET":"DRY",sizing:maxAction===0?"NONE":ratio<=.33?"SMALL":ratio<=.7?"MEDIUM":ratio<=1?"LARGE":"OVERBET",tournamentPhase:has(spot,"EARLY")?"EARLY":has(spot,"MID")?"MID":has(spot,"BOLHA")?"BUBBLE":has(spot,"ITM")?"ITM":has(spot,"FT")?"FT":"NA",icm:has(spot,"ICM"),ante:has(spot,"ANTE"),gameProfile:spot.gameProfile??spot.mode,anteMode:spot.anteMode??"NONE"}}
function generatedPrompt(players:Array<{position:string;action:string;value:number;hero?:boolean}>,heroPosition:string){const villains=players.filter(p=>!p.hero).map(p=>`${p.position} ${p.action}${p.value>0?` ${p.value}`:""}`).join(" · ");return `AÇÃO ATÉ O HERÓI: ${villains}. HERO EM ${heroPosition}. QUAL É A MELHOR DECISÃO?`}
function legalHeroActions(street:Street,players:PlayerDnaSpot["players"]):PlayerAction[]{const h=players.find(player=>player.hero);if(!h)return[];const activeVillains=players.filter(player=>!player.hero&&player.action!=="FOLD");const highest=Math.max(0,...activeVillains.map(player=>Math.max(0,player.value)));const outstanding=Math.max(0,highest-Math.max(0,h.value));if(outstanding>0){if(h.stack<=outstanding)return["FOLD","CALL"];return["FOLD","CALL","RAISE","ALL-IN"]}return street==="PREFLOP"?["CHECK","RAISE","ALL-IN"]:["CHECK","BET","ALL-IN"]}
function makeVariant(template:PlayerDnaSpot,slot:number,sessionSeed:number,answers:PriorAnswer[]):PlayerDnaSpot{const adaptation=answers.slice(-6).map(answer=>answer.action).join("|")||"START";const random=randomFrom(hashText(`${template.id}:${sessionSeed}:${slot}:${adaptation}`));const suitMap=suitPermutation(random);const heroCards=remapCards(template.heroCards,suitMap)??template.heroCards;const board=remapCards(template.board,suitMap);const stackFactor=.90+random()*.20;const valueFactor=.92+random()*.16;const hasSidePots=Boolean(template.pot.sides?.length);const templateVillainCommitted=template.players.filter(player=>!player.hero).reduce((sum,player)=>sum+Math.max(0,player.value),0);const usedCards=new Set([...splitCards(heroCards),...splitCards(board)]);const players=template.players.map(player=>{const stack=round(player.stack*stackFactor,template.mode);if(player.hero)return{...player,stack,value:capCommitment(player.value*valueFactor,stack,template.mode),action:"---"};const policy=solverNodePolicy(template,player.position,player.action);const sampled=hasSidePots?normalizedAction(player.action):sampleSolverAction(policy,random);const action=hasSidePots?player.action:solverActionLabel(sampled,player.action);const templateValue=player.value>0?round(player.value*valueFactor,template.mode):0;const rawValue=hasSidePots?templateValue:sampled==="CHECK"||sampled==="FOLD"?0:sampled==="ALL-IN"?stack:templateValue>0?templateValue:round(template.pot.main*(sampled==="BET"?.5:.75),template.mode);const value=capCommitment(rawValue,stack,template.mode);const available=expandWeightedRange(policy.range,[...usedCards]);const conditioned=conditionRangeOnNodeAction(available,sampled,policy.actions);const combo=sampleWeightedCombo(conditioned,random);if(combo){usedCards.add(combo.cards[0]);usedCards.add(combo.cards[1])}return{...player,stack,value,action,rangeProfile:policy.range,solverActionFrequency:policy.actions[sampled]??0,solverNodeSource:policy.source,sampledCombo:combo?comboKey(combo.cards):undefined,rangeComboCount:available.length,conditionedRangeComboCount:conditioned.length,conditionedComboWeight:combo?Math.round(combo.weight*1_000_000)/1_000_000:undefined,comboActionLikelihood:combo?Math.round(combo.actionLikelihood*1_000_000)/1_000_000:undefined}});const scaledTemplatePot=round(template.pot.main*valueFactor,template.mode);const newVillainCommitted=players.filter(player=>!player.hero).reduce((sum,player)=>sum+Math.max(0,player.value),0);const scaledOldCommitted=round(templateVillainCommitted*valueFactor,template.mode);const commitmentDelta=hasSidePots?0:newVillainCommitted-scaledOldCommitted;const mainPot=round(Math.max(0.1,scaledTemplatePot+commitmentDelta),template.mode);const pot={main:mainPot,...(template.pot.sides?.length?{sides:template.pot.sides.map(side=>({...side,value:round(side.value*valueFactor,template.mode)}))}:{})};const h=players.find(player=>player.hero)??players[0];const actions=legalHeroActions(template.street,players);return{...template,id:`${template.id}-${sessionSeed.toString(36)}-${slot.toString(36)}-${hashText(adaptation).toString(36)}`,heroCards,board,players,pot,prompt:generatedPrompt(players,h.position),actions,scenario:[...template.scenario]}}
function isMultiwaySeed(spot:PlayerDnaSpot){return describeSpot(spot).heads==="MULTIWAY"}
function multiwayOffset(seed:number,mode:GameMode){return hashText(`${seed}:${mode}:MULTIWAY`)%MULTIWAY_INTERVAL}
function isMultiwaySlot(slot:number,offset:number){return(slot+offset)%MULTIWAY_INTERVAL===0}
function pickTemplate(ordered:PlayerDnaSpot[],slot:number,seed:number,prior:PriorAnswer[]){const adaptation=prior.slice(-6).map(answer=>answer.action).join("|")||"START";return ordered[(slot+hashText(`${seed}:${adaptation}`))%ordered.length]}
function buildTemplateBalancedSession(seeds:PlayerDnaSpot[],mode:GameMode,count:number,seed:number,answers:PriorAnswer[]){const multiwaySeeds=seeds.filter(isMultiwaySeed);const headsUpSeeds=seeds.filter(spot=>!isMultiwaySeed(spot));const multiwayOrdered=stableShuffle(multiwaySeeds,seed^0x85ebca6b);const headsUpOrdered=stableShuffle(headsUpSeeds.length?headsUpSeeds:seeds,seed);const offset=multiwayOffset(seed,mode);const output:PlayerDnaSpot[]=[];for(let slot=0;slot<count;slot++){const prior=answers.slice(0,slot);const useMultiway=multiwayOrdered.length>0&&isMultiwaySlot(slot,offset);const pool=useMultiway?multiwayOrdered:headsUpOrdered;const template=pickTemplate(pool,slot,seed,prior);output.push(makeVariant(template,slot,seed,prior))}return output}
export function buildBalancedSpotSession(bank:PlayerDnaSpot[],mode:GameMode,count:number,seed=Date.now(),answers:PriorAnswer[]=[]):PlayerDnaSpot[]{if(count<=0)return[];const seeds=bank.filter(spot=>spot.mode===mode);if(!seeds.length)return[];const multiwaySeeds=seeds.filter(isMultiwaySeed);if(runtimeBankReady()&&multiwaySeeds.length){const offset=multiwayOffset(seed,mode);const multiwayCount=Array.from({length:count},(_,slot)=>slot).filter(slot=>isMultiwaySlot(slot,offset)).length;const exactCount=count-multiwayCount;const exact=buildExactComboSession(runtimeExactBank,mode,exactCount,seed);if(exact.coveragePct===100){const multiwayOrdered=stableShuffle(multiwaySeeds,seed^0x85ebca6b);const output:PlayerDnaSpot[]=[];let exactIndex=0;for(let slot=0;slot<count;slot++){if(isMultiwaySlot(slot,offset)){const prior=answers.slice(0,slot);const template=pickTemplate(multiwayOrdered,slot,seed^0x27d4eb2f,prior);output.push(makeVariant(template,slot,seed^0x27d4eb2f,prior))}else{const exactSpot=exact.spots[exactIndex++];if(exactSpot)output.push(exactSpot)}}if(output.length===count)return output}}if(runtimeBankReady()){const exact=buildExactComboSession(runtimeExactBank,mode,count,seed);if(exact.coveragePct===100)return exact.spots}return buildTemplateBalancedSession(seeds,mode,count,seed,answers)}

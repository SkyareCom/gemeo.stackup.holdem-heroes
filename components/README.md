# Shared UI components

`SharedPokerTable` is the canonical table surface for Player Profile, Hand Analysis and Poker Math practice. Do not create module-specific duplicate table implementations.

`MathWorkspace` renders the complete math concept library from `data/math-concepts.ts`, including search, category filtering, concept details and deterministic practice calculations.

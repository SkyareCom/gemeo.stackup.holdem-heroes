export type SharedPokerTableProps={villain?:string;hero?:string;board?:string[];pot?:string|number};
export default function SharedPokerTable({villain="VILLAIN · CO",hero="HERO · BTN · A♦ K♦",board=["A♠","7♥","2♣"],pot="—"}:SharedPokerTableProps){const cards=[...board];while(cards.length<5)cards.push("?");return <div className="table-wrap"><div className="poker-table"><span className="seat top">{villain}</span><div className="board">{cards.slice(0,5).map((card,i)=>card==="?"?<i key={i}>?</i>:<b key={`${card}-${i}`}>{card}</b>)}</div><div className="pot">POT <strong>{pot}</strong></div><span className="seat bottom">{hero}</span></div></div>}
